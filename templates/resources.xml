<?xml version="1.0" encoding="UTF-8"?>
<Resources>
    {% for res in resources %}
        <Resource id="{{res.id}}">
            <title>{{res.name}}</title>
            <url>{{res.url}}</url>
            <desc>{{res.desc}}</desc>
            <keywords>
                {% for key in res.keyword_set.all %}
                    <keyword>{{key}}</keyword>
                {% endfor %}
            </keywords>
            <subresources>
            {{sr}}
                {% for sr in res.child.all %}
                    <subresource id="{{sr.id}}">
                        <title>{{sr.title}}</title>
                        <url>{{sr.href}}</url>
                        <desc>{{sr.desc}}</desc>
                    </subresource>
                {% endfor %}
            </subresources>
        </Resource>
    {% endfor %}
</Resources>
